Azure OIDC Workflow Fix - Summary
Date: 2026-04-06

What I changed
- Split `.github/workflows/azure-dev.yml` into two jobs:
  - `build-main` (runs for `workflow_run` from CI on `main`) — no `environment` binding.
  - `build-dispatch` (runs for `workflow_dispatch`) — keeps `environment: production`.
  - Commit created on branch `fix/ci-azure-oidc-workflow`.
    - Commit: 62fc7b4e7b93ab752453198f5650a3dfdcae3c31

Azure identity work performed
- Target managed identity: `aca_env_mi-eftsgirczttfc`
- Resource group: `rg-walrhouse-core2`
- Managed identity clientId: `e1041e50-0204-4add-82cd-b27e0449125e`

Federated credentials added (to allow both environment and branch OIDC subjects)
- `github-production-env`
  - subject: `repo:VulpritProoze/Walrhouse:environment:production`
  - issuer: `https://token.actions.githubusercontent.com`
  - audiences: `api://AzureADTokenExchange`
- `github-main-ref`
  - subject: `repo:VulpritProoze/Walrhouse:ref:refs/heads/main`
  - issuer: `https://token.actions.githubusercontent.com`
  - audiences: `api://AzureADTokenExchange`

Commands I ran (high-level)
- Local repo changes:
  git checkout -b fix/ci-azure-oidc-workflow
  git add .github/workflows/azure-dev.yml
  git commit -m "fix(ci): split azure-dev workflow into build-main and build-dispatch"

- Azure CLI (interactive MFA login required):
  az login --tenant <tenant-id> --scope https://graph.microsoft.com//.default --scope https://management.core.windows.net//.default

- Managed identity discovery and federated credential creation (ran from PowerShell):
  az identity show --name aca_env_mi-eftsgirczttfc --resource-group rg-walrhouse-core2 --query clientId -o tsv
  az identity federated-credential create --name github-production-env --identity-name aca_env_mi-eftsgirczttfc --resource-group rg-walrhouse-core2 --issuer https://token.actions.githubusercontent.com --subject repo:VulpritProoze/Walrhouse:environment:production --audiences api://AzureADTokenExchange
  az identity federated-credential create --name github-main-ref --identity-name aca_env_mi-eftsgirczttfc --resource-group rg-walrhouse-core2 --issuer https://token.actions.githubusercontent.com --subject repo:VulpritProoze/Walrhouse:ref:refs/heads/main --audiences api://AzureADTokenExchange
  az identity federated-credential list --identity-name aca_env_mi-eftsgirczttfc --resource-group rg-walrhouse-core2

Notes / next steps for you
- Push branch and open a PR: `git push -u origin fix/ci-azure-oidc-workflow` and open a PR to merge to `main`.
- Confirm the repository `vars`/secrets are correct (particularly `AZURE_CLIENT_ID` points to `e1041e50-0204-4add-82cd-b27e0449125e`).
- Rerun the workflow (either wait for CI-triggered run or manually dispatch) to verify deployment succeeds.

If anything fails when you rerun the workflow, paste the workflow job logs and I will diagnose next steps.
